<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>LIA Protection Gap Calculator - Coverage Clarity Assessment™</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { 
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; 
            max-width: 800px; 
            margin: 0 auto; 
            padding: 2rem; 
            line-height: 1.6; 
            background: #f8fafc;
            color: #1e293b;
        }
        .header { 
            text-align: center; 
            margin-bottom: 3rem; 
            color: #1e40af;
        }
        .header h1 { 
            font-size: 2.5rem; 
            margin-bottom: 0.5rem; 
        }
        .intro { 
            background: white; 
            padding: 2.5rem; 
            border-radius: 12px; 
            box-shadow: 0 4px 6px rgba(0,0,0,0.1); 
            margin-bottom: 2rem;
            border-left: 5px solid #1e40af;
        }
        .intro h2 { 
            color: #dc2626; 
            font-size: 1.5rem; 
            margin-bottom: 1rem;
            font-weight: 700;
        }
        .intro strong { color: #1e40af; }
        .input-group { 
            margin-bottom: 1.5rem; 
            padding: 1.5rem; 
            background: #f1f5f9; 
            border-radius: 8px; 
            border-left: 4px solid #3b82f6;
        }
        .input-group h3 { 
            color: #1e40af; 
            margin-bottom: 1rem; 
            font-size: 1.1rem;
        }
        .input-row { display: flex; gap: 1rem; align-items: end; flex-wrap: wrap; }
        input[type="number"], select { 
            padding: 0.75rem; 
            border: 2px solid #e2e8f0; 
            border-radius: 8px; 
            font-size: 1rem;
            min-width: 120px;
        }
        input[type="number"]:focus, select:focus { 
            outline: none; 
            border-color: #3b82f6; 
            box-shadow: 0 0 0 3px rgba(59,130,246,0.1);
        }
        .question-section { 
            background: white; 
            padding: 2rem; 
            border-radius: 12px; 
            box-shadow: 0 4px 6px rgba(0,0,0,0.1); 
            margin-bottom: 2rem;
        }
        .radio-group { display: flex; gap: 1rem; flex-wrap: wrap; }
        input[type="radio"] { margin-right: 0.5rem; transform: scale(1.2); }
        label { 
            display: flex; 
            align-items: center; 
            padding: 0.75rem 1.25rem; 
            background: white; 
            border-radius: 8px; 
            cursor: pointer; 
            transition: all 0.2s;
            border: 2px solid transparent;
            font-weight: 500;
        }
        input[type="radio"]:checked + label { 
            background: #dbeafe; 
            border-color: #3b82f6; 
            box-shadow: 0 0 0 3px rgba(59,130,246,0.1);
        }
        .score-display { 
            font-size: 4rem; 
            font-weight: 800; 
            text-align: center; 
            margin: 2rem 0;
        }
        .score-gap { color: #dc2626; background: #fef2f2; }
        .score-moderate { color: #ca8a04; background: #fef3c7; }
        .score-well { color: #16a34a; background: #f0fdf4; }
        .protection-level { 
            font-size: 1.8rem; 
            text-align: center; 
            margin: 1rem 0;
            font-weight: 700;
        }
        .cta { 
            display: block;
            text-align: center; 
            padding: 1.5rem 2rem; 
            background: linear-gradient(135deg, #1e40af, #3b82f6); 
            color: white; 
            border-radius: 12px; 
            text-decoration: none;
            font-weight: 700;
            font-size: 1.1rem;
            margin-top: 2rem;
            transition: all 0.3s;
        }
        .cta:hover { 
            transform: translateY(-3px); 
            box-shadow: 0 15px 35px rgba(30,64,175,0.4); 
        }
        button { 
            padding: 1.25rem 3rem; 
            font-size: 1.2rem; 
            background: #10b981; 
            color: white; 
            border: none; 
            border-radius: 12px; 
            cursor: pointer;
            font-weight: 700;
            display: block;
            margin: 2rem auto;
        }
        button:hover { background: #059669; transform: translateY(-2px); }
    </style>
</head>
<body>
    <div class="header">
        <h1>🛡️ Coverage Clarity Assessment™</h1>
        <p><strong>Powered by LIA Protection Gap methodology — See if you're Well Protected, Moderately Protected, or Facing a Gap</strong></p>
    </div>

    <!-- Intro Copy (Havend Style) -->
    <div class="intro">
        <h2>No Mis-selling. No Over-selling. No Pressure-selling.</h2>
        <p style="margin-bottom: 1rem; font-size: 1.1rem;">
            <em>(We might sound like a broken record, but it's because you deserve better.)</em>
        </p>
        <p style="margin-bottom: 1rem;">
            You're not here for a sales pitch. You just want to be <strong>sure</strong> your insurance works for you — not against you.
        </p>
        <p style="margin-bottom: 1.5rem;">
            <strong>Too many people buy coverage they don't understand.</strong> Premiums drain month after month, but when claims come… the fine print disappoints.
        </p>
        <p style="margin-bottom: 2rem;">
            <strong>Our promise:</strong> Honest clarity using LIA Singapore's proven Protection Gap methodology. No product pushing. Just the truth about your coverage.
        </p>
    </div>

    <form id="protectionGapTest">
        <!-- Personal Details (LIA Style) -->
        <div class="question-section">
            <h2 style="color: #1e40af; margin-bottom: 1.5rem;">About You</h2>
            
            <div class="input-group">
                <h3>Your Age</h3>
                <input type="number" id="age" min="20" max="80" value="35" placeholder="e.g. 35">
            </div>

            <div class="input-group">
                <h3>Annual Household Income (S$)</h3>
                <input type="number" id="income" min="0" value="120000" placeholder="e.g. 120,000">
            </div>

            <div class="input-group">
                <h3>Current Insurance Coverage (S$)</h3>
                <div class="input-row">
                    <div>
                        <label>Death/TPD:</label>
                        <input type="number" id="deathCoverage" min="0" value="300000" placeholder="e.g. 300,000">
                    </div>
                    <div>
                        <label>Critical Illness:</label>
                        <input type="number" id="ciCoverage" min="0" value="200000" placeholder="e.g. 200,000">
                    </div>
                </div>
            </div>
        </div>

        <!-- Protection Needs Assessment -->
        <div class="question-section">
            <h2 style="color: #1e40af; margin-bottom: 1.5rem;">Your Protection Needs</h2>
            
            <div class="input-group">
                <h3>Number of Dependants</h3>
                <select id="dependants">
                    <option value="0">0</option>
                    <option value="1">1</option>
                    <option value="2" selected>2</option>
                    <option value="3">3</option>
                    <option value="4+">4+</option>
                </select>
            </div>

            <div class="input-group">
                <h3>Outstanding Debts (Loans + Mortgage) (S$)</h3>
                <input type="number" id="debts" min="0" value="200000" placeholder="e.g. 200,000">
            </div>

            <div class="input-group">
                <h3>Annual Household Expenses (S$)</h3>
                <input type="number" id="expenses" min="0" value="60000" placeholder="e.g. 60,000">
            </div>
        </div>

        <!-- Lifestyle & Coverage Questions -->
        <div class="question-section">
            <h2 style="color: #1e40af; margin-bottom: 1.5rem;">Lifestyle & Coverage Check</h2>
            
            <div class="input-group">
                <h3>Marital Status</h3>
                <div class="radio-group">
                    <input type="radio" name="marital" value="married" id="married" checked>
                    <label for="married">Married</label>
                    <input type="radio" name="marital" value="single" id="single">
                    <label for="single">Single</label>
                </div>
            </div>

            <div class="input-group">
                <h3>Has coverage been reviewed in last 12 months?</h3>
                <div class="radio-group">
                    <input type="radio" name="reviewed" value="yes" id="reviewed-yes">
                    <label for="reviewed-yes">✅ Yes</label>
                    <input type="radio" name="reviewed" value="no" id="reviewed-no" checked>
                    <label for="reviewed-no">❌ No</label>
                </div>
            </div>

            <div class="input-group">
                <h3>Total annual premiums as % of income?</h3>
                <div class="radio-group">
                    <input type="radio" name="premiumRatio" value="low" id="low" checked>
                    <label for="low">✅ <2% (Efficient)</label>
                    <input type="radio" name="premiumRatio" value="high" id="high">
                    <label for="high">❌ >2% (Review needed)</label>
                </div>
            </div>
        </div>

        <button type="button" onclick="calculateLiaScore()">🧮 Calculate My Protection Level</button>
    </form>

    <div id="scoreResult" style="display: none;" class="question-section">
        <div class="score-display" id="scoreNumber"></div>
        <div class="protection-level" id="protectionLevel"></div>
        <div id="scoreMessage" style="text-align: center; font-size: 1.3rem; margin: 1.5rem 0; padding: 1.5rem; border-radius: 8px;"></div>
        <p style="text-align: center; margin-bottom: 2rem; font-size: 1.1rem;">
            <strong>Want your personalized LIA-style gap analysis?</strong>
        </p>
        <a href="https://havend.com/#contact" class="cta">
            📋 Get Your Free Detailed Protection Review →
        </a>
    </div>

    <script>
        function calculateLiaScore() {
            // Get form values
            const age = parseInt(document.getElementById('age').value) || 35;
            const income = parseInt(document.getElementById('income').value) || 120000;
            const deathCoverage = parseInt(document.getElementById('deathCoverage').value) || 300000;
            const ciCoverage = parseInt(document.getElementById('ciCoverage').value) || 200000;
            const dependants = parseInt(document.getElementById('dependants').value) || 2;
            const debts = parseInt(document.getElementById('debts').value) || 200000;
            const expenses = parseInt(document.getElementById('expenses').value) || 60000;
            
            const formData = new FormData(document.getElementById('protectionGapTest'));
            const marital = formData.get('marital') === 'married' ? 1 : 0;
            const reviewed = formData.get('reviewed') === 'yes' ? 1 : 0;
            const premiumRatio = formData.get('premiumRatio') === 'low' ? 1 : 0;

            // LIA-style calculation: Resources vs Overheads
            const protectionNeeds = debts + (expenses * 10) + (income * dependants * 0.6); // Simplified LIA formula
            const resourcesAvailable = deathCoverage + ciCoverage + (income * 0.3); // Existing coverage + buffer
            
            const protectionRatio = Math.min(100, (resourcesAvailable / protectionNeeds) * 100);
            
            // Additional factors (LIA-style)
            const ageFactor = Math.max(0, 100 - (age - 30) * 2); // Age penalty
            const reviewFactor = reviewed * 15;
            const premiumFactor = premiumRatio * 10;
            
            const finalScore = Math.round((protectionRatio * 0.7 + ageFactor * 0.1 + reviewFactor + premiumFactor) / 1.8);

            // Display LIA-style results
            const scoreElement = document.getElementById('scoreNumber');
            const levelElement = document.getElementById('protectionLevel');
            const messageElement = document.getElementById('scoreMessage');
            const resultSection = document.getElementById('scoreResult');

            scoreElement.textContent = finalScore + '%';
            scoreElement.className = 'score-display';

            let levelText = '';
            let message = '';
            let scoreClass = '';

            if (finalScore < 60) {
                levelText = 'Facing a Protection Gap';
                message = 'Please note that you are currently facing a protection gap as the value of your resources is lower than your overheads.<br><strong>Your protection level is below the baseline of 60%</strong><br>Please take action today to narrow your protection gap.';
                scoreClass = 'score-gap';
            } else if (finalScore < 75) {
                levelText = 'Moderately Protected';
                message = 'Well done! You are currently moderately protected as your resources are higher than your overheads.<br><strong>Your protection level has met the baseline of 60%</strong><br>Please remain vigilant and review regularly.';
                scoreClass = 'score-moderate';
            } else {
                levelText = 'Well Protected';
                message = `Congratulations! You are currently well-protected as your resources are much higher than your overheads.<br><strong>Your protection level has exceeded the baseline of 60% by ${Math.round((finalScore-60)/5)}%</strong><br>Please remain vigilant and review regularly.`;
                scoreClass = 'score-well';
            }

            scoreElement.classList.add(scoreClass);
            levelElement.textContent = levelText;
            messageElement.innerHTML = message;
            resultSection.style.display = 'block';
            resultSection.scrollIntoView({ behavior: 'smooth' });
        }
    </script>
</body>
</html>
